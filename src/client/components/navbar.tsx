import type { NavLink, SiteConfig } from "../../utils/config.ts";

export const Navbar = ({
  links,
  socialsLinks,
}: {
  socialsLinks: SiteConfig["socialsLinks"];
  links: NavLink[];
}) => {
  return <>daw</>;
};
