import { Button } from "@mantine/core";

export const HomePage = ({ meta }: { meta: any }) => {
  return (
    <div>
      <Button variant="filled" radius="lg">
        Button
      </Button>
    </div>
  );
};
